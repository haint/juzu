<arquillian xmlns="http://jboss.org/schema/arquillian"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="
        http://jboss.org/schema/arquillian
        http://jboss.org/schema/arquillian/arquillian_1_0.xsd">
        
    <container qualifier="tomcat7">
      <configuration>
        <property name="catalinaHome">${container.home}</property>
        <property name="user">${tomcat.user}</property>
        <property name="pass">${tomcat.password}</property>
      </configuration>
    </container>

    <container qualifier="gatein">
      <configuration>
        <property name="catalinaHome">${container.home}</property>
        <property name="javaVmArguments">-Xms128m -Xmx384m -XX:MaxPermSize=192m  -Dorg.apache.commons.logging.Log=org.apache.commons.logging.impl.SimpleLog -Djava.security.auth.login.config=../conf/jaas.conf -Dexo.product.developing=false -Dexo.conf.dir.name=gatein/conf -Djava.awt.headless=true  -Djava.util.logging.manager=org.apache.juli.ClassLoaderLogManager</property>
        <property name="user">${tomcat.user}</property>
        <property name="pass">${tomcat.password}</property>
      </configuration>
    </container>
  
</arquillian>